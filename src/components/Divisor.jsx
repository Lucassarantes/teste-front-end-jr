import "../styles/Divisor.scss";

const Divisor = ({ texto }) => {
  return (
    <div id="divisor">
        <h6 className='separator titulo-secao'>{texto}</h6>
    </div>
  )
}

export default Divisor